# Browser E2E Evidence - 2026-05-04

## Verdict

`BLOCKED`

`BROWSER_E2E_DONE` was not reached because the authorized `/control-ui/` routes returned HTTP `502` before browser-visible GTClaw DOM could render.

## Gate Statement

Serial Browser E2E gate executed as EvidenceWorker with no new agent.

Only this repository file was written:

`specs/gtclaw-runtime-controlui-persistent-image/evidence/20260504-browser-e2e.md`

No pod, instance, runtime image/resource setting, Kubernetes resource, database, registry, image, or tag was created, deleted, restarted, patched, or modified. No pod files were read or written. No `kubectl cp` was used. No backend, frontend, deployment, docs, longterm, AgentTeam, spec, plan, tasks, or existing evidence file was modified. No Mem0 write, no longterm write, no passes:true, no Close.

## Dependency Evidence

| Dependency | Evidence file | Result used |
| --- | --- | --- |
| Browser E2E approval packet | `specs/gtclaw-runtime-controlui-persistent-image/evidence/20260504-browser-e2e-approval-packet.md` | Browser E2E approved for instance `5` only |
| Fresh pod hash evidence | `specs/gtclaw-runtime-controlui-persistent-image/evidence/20260504-fresh-instance-mutation-and-pod-hash.md` | Fresh deployed pod hash matched all four allowlist files |

## Authorized Target

| Field | Value |
| --- | --- |
| Instance | `5` / `gtclaw-fresh-20260504095843` |
| Pod | `clawmanager-user-1/clawreef-5-gtclaw-fresh-20260504095843` / `desktop` |
| Image | `k3d-clawmanager-registry:5000/clawmanager-openclaw/openclaw:gtclaw-controlui-persistent-20260504005506` |

## Tooling

Local Playwright was not available in the current workspace or global Node environment. Browser DOM checks were executed with a clean headless Google Chrome session through Chrome DevTools Protocol. This still used a real browser DOM surface and route-scoped cookies. The transient browser profile was removed after probing.

## Secret Hygiene

No token value, no cookie value, no credential, no secret, no `.env`, no `.codex/auth.json`, no `.codex/config.toml`, and no token-bearing URL was recorded.

Route-scoped tokens and cookies were used only transiently in process memory. Evidence records only safe route labels, cookie presence metadata, access modes, target ports, and browser DOM outcomes.

## API Preflight

| Check | Result |
| --- | --- |
| Admin authentication | HTTP `200`; authenticated; credential not recorded |
| Instance lookup | HTTP `200`; id `5`; name `gtclaw-fresh-20260504095843`; status `running`; type `openclaw` |
| Pod identity from API | pod name matched `clawreef-5-gtclaw-fresh-20260504095843`; namespace matched `clawmanager-user-1` |
| `/api/v1/instances/5/access?mode=control-ui` | HTTP `200`; access mode `control-ui`; target port `18789`; safe path `/api/v1/instances/5/control-ui/`; token value not recorded; cookie value not recorded |
| `/api/v1/instances/5/access?mode=desktop` | HTTP `200`; access mode `desktop`; target port `3001`; safe path `/api/v1/instances/5/proxy/`; token value not recorded; cookie value not recorded |

## Browser Control-UI DOM Checks

Required route set: `/control-ui/`, `/control-ui/chat?session=main`, and one `/control-ui/` history fallback.

| Route label | Safe requested path | Browser document HTTP | Final browser path | `GTClaw 控制台` in visible DOM body | `zh-CN` lang | Chinese markers `控制台`, `网关`, `连接` | Desktop fallback | 404 marker | Result |
| --- | --- | ---: | --- | --- | --- | --- | --- | --- | --- |
| `/control-ui/` root | `/api/v1/instances/5/control-ui/` | `502` | `/api/v1/instances/5/control-ui/` | no | no | missing | no | no | fail |
| `/control-ui/chat?session=main` | `/api/v1/instances/5/control-ui/chat?session=main` | `502` | `/api/v1/instances/5/control-ui/chat?session=main` | no | no | missing | no | no | fail |
| `/control-ui/` history fallback | `/api/v1/instances/5/control-ui/history-fallback-check` | `502` | `/api/v1/instances/5/control-ui/history-fallback-check` | no | no | missing | no | no | fail |

Observed browser body on all three failing control-ui routes was a proxy error, not the GTClaw control console. The browser-visible failure class was `Failed to proxy request ... connect: connection refused` for the control-ui target port `18789`.

## Desktop Regression

`access?mode=desktop` and `/proxy/` remained desktop behavior.

| Check | Result |
| --- | --- |
| `access?mode=desktop` API contract | HTTP `200`; access mode `desktop`; target port `3001`; safe path `/api/v1/instances/5/proxy/` |
| `/proxy/` browser final path | `/api/v1/instances/5/proxy/` |
| `/proxy/` browser title | `ClawManager Desktop` |
| `/proxy/` browser lang | `en` |
| `/proxy/` visible body | desktop controls visible; body length `97` |
| `/proxy/` GTClaw control-ui replacement | no `GTClaw 控制台` in visible DOM body |
| desktop regression verdict | pass |

## Required Criteria Summary

| Criterion | Result |
| --- | --- |
| control-ui routes visible DOM body renders `GTClaw 控制台` | fail: all three returned HTTP `502` |
| `lang=zh-CN` or equivalent DOM evidence | fail: no control-ui DOM rendered |
| Chinese markers `控制台`, `网关`, `连接` | fail: no control-ui DOM rendered |
| control-ui routes have no desktop fallback | pass: no desktop fallback observed |
| control-ui routes have no 404 marker | pass: HTTP `502`, not 404 |
| `access?mode=desktop` keeps desktop behavior | pass |
| `/proxy/` keeps desktop behavior | pass |
| no token value | pass |
| no cookie value | pass |
| no token-bearing URL | pass |
| no passes:true | pass |
| no Close | pass |

## Blocker

The fresh instance and access contract were reachable, but the authorized GTManager `/control-ui/` proxy could not connect to the runtime control-ui upstream on target port `18789`. Because all three required control-ui browser DOM routes returned `502`, this gate must remain `BLOCKED` until the runtime control-ui upstream is reachable through the authorized route.
