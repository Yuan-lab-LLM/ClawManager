# GTClaw Runtime Control-UI Persistent Image Delivery Tasks

> Tasks-only draft. Commander/user gate currently authorizes this file only. Do not execute these tasks from this artifact without the approval gates named below.

## Gate Summary

- Current artifact scope: write `specs/gtclaw-runtime-controlui-persistent-image/tasks.md` only.
- Current reviewed inputs: `spec.md`, `plan.md`, FR-011 path/source-of-truth requirement, F-006 historical evidence, T8I evidence, runtime control-ui body repair addendum, and test-environment mutation record.
- Default topology: serial. Only read-only tasks explicitly tagged `parallel-safe` may be scheduled in parallel by Commander.
- Historical evidence boundary: F-006 T8I and the addendum are baseline only. They do not prove this feature's persistence because they used an existing disposable runtime state and a disposable pod repair record.
- Global current prohibitions: no implementation, image build/tag/push/pull, runtime/container mutation, registry mutation, Kubernetes resource/Secret/ConfigMap/namespace mutation, database mutation, fresh instance creation, authorized browser E2E, `passes:true`, or Close/write-back.
- Secret hygiene: no token value, cookie value, credential, secret, `.env`, `.codex/auth.json`, `.codex/config.toml`, full environment dump, or token-bearing URL may be recorded.

## 1. Preflight / Read-only Review

- [ ] **T001 - CommanderPreflightWorker: confirm current task gate and source set.** Worker: CommanderPreflightWorker. Read-only: yes. Explicit approval required: no, provided it only reads the named files and writes no file other than this future checklist state if later authorized. Allowed files/resources: `AGENTS.md`, `.specify/memory/constitution.md`, `backend/AGENTS.md`, `longterm/workspace/app_spec.md`, `longterm/workspace/feature_list.json`, `AgentTeam/01-Commander-SOP.md`, `AgentTeam/03-Dispatch-Templates.md`, `specs/gtclaw-runtime-controlui-persistent-image/spec.md`, `specs/gtclaw-runtime-controlui-persistent-image/plan.md`, the three named historical evidence files. Forbidden actions: implementation, image pull/build/tag/push, runtime/K8S/registry/database mutation, fresh instance creation, browser E2E, `passes:true`, Close. Scheduling: serial by default, parallel-safe only with T002/T003.
- [ ] **T002 - CommanderPreflightWorker: capture dirty worktree scope without expanding ownership.** Worker: CommanderPreflightWorker. Read-only: yes. Explicit approval required: no. Allowed files/resources: `git status --short` and path-limited status for `spec.md`, `plan.md`, `tasks.md`. Forbidden actions: modifying, staging, reverting, committing, deleting, or normalizing unrelated dirty files; touching backend/frontend/deployments/docs/longterm/AgentTeam; `passes:true`; Close. Scheduling: serial by default, parallel-safe only with T001/T003.
- [ ] **T003 - CommanderPreflightWorker: classify F-006 historical evidence as baseline only.** Worker: CommanderPreflightWorker. Read-only: yes. Explicit approval required: no. Allowed files/resources: `20260502-t8i-e2e-rerun-after-runtime-body-fix.md`, `20260503-runtime-controlui-body-repair-manifest-addendum.md`, `20260501001159-test-env-mutation.md`. Forbidden actions: treating old T8I/addendum as this feature persistence proof, changing old evidence, reusing old disposable pod state as fresh deployed proof, `passes:true`, Close. Scheduling: serial by default, parallel-safe only with T001/T002.

## 2. PathVerificationWorker Task

- [ ] **T004 - PathVerificationWorker: prove source-of-truth relationship between control-ui paths.** Worker: PathVerificationWorker. Read-only: yes. Explicit approval required: Commander/user approval is required before any runtime access method that Commander classifies as pod/container access under the active gate; stop if approval is missing. Allowed files/resources after approval: read-only metadata for `/usr/local/lib/node_modules/openclaw/dist/control-ui` and `/opt/opensparrow/runtime/openclaw/dist/control-ui`; package metadata for `openclaw`; launcher/process arguments that identify the served static directory; no file writes. Forbidden actions: image pull/build/tag/push, `kubectl cp`, writing to runtime files, restarting processes, patching pods, mutating K8S resources, recording token/cookie/secret data, `passes:true`, Close. Scheduling: serial, required before delivery decision.
- [ ] **T005 - PathVerificationWorker: record path evidence fields.** Worker: PathVerificationWorker. Read-only: yes. Explicit approval required: same as T004. Allowed files/resources after approval: read-only `readlink` result, inode, device, file type, package name/version, launcher args, runtime listener/process metadata, and served-file SHA-256/size for one allowlist file requested through the control-ui route. Forbidden actions: modifying either path, replacing files, broad scanning outside the two paths except minimal package/process metadata, outputting token-bearing URLs, `passes:true`, Close. Scheduling: serial after T004.
- [ ] **T006 - PathVerificationWorker: enforce copy-target failure condition.** Worker: PathVerificationWorker. Read-only: yes. Explicit approval required: no for writing the finding to a future evidence draft; yes for any runtime probe under T004/T005. Allowed files/resources: path proof notes in a future evidence packet. Forbidden actions: continuing to image/resource delivery if the copy target cannot be proven as the runtime-served path; accepting `/opt/opensparrow/runtime/openclaw/dist/control-ui` or `/usr/local/lib/node_modules/openclaw/dist/control-ui` by assumption; manual pod patch as persistence evidence; `passes:true`; Close. Scheduling: serial gate.
- [ ] **T007 - PathVerificationWorker: stop criteria for mutation needs.** Worker: PathVerificationWorker. Read-only: yes. Explicit approval required: no to stop and report; explicit Commander/user approval required before any next step. Allowed files/resources: gate report only. Forbidden actions: image pull/build/tag/push, runtime mutation, K8S/resource mutation, registry mutation, database mutation, fresh instance creation, browser E2E, `passes:true`, Close. Scheduling: serial gate.

## 3. Delivery Decision Gate

- [ ] **T008 - CommanderDecisionGateWorker: choose delivery type only after path proof.** Worker: CommanderDecisionGateWorker. Read-only: yes. Explicit approval required: yes for any choice that authorizes later implementation. Allowed files/resources: `spec.md`, `plan.md`, `tasks.md`, PathVerificationWorker evidence. Forbidden actions: defaulting to equivalent resource delivery, changing manifests, creating tags, mutating registries/resources, `passes:true`, Close. Scheduling: serial after T004-T007.
- [ ] **T009 - CommanderDecisionGateWorker: keep default strategy as digest-pinned runtime image/tag.** Worker: CommanderDecisionGateWorker. Read-only: yes. Explicit approval required: yes before future image work. Allowed files/resources: decision notes identifying base digest, target tag label, image index digest expectation, linux/arm64 platform digest expectation. Forbidden actions: mutable tag-only identity, broad OpenClaw -> GTClaw replacement, deployment default changes, registry mutation, `passes:true`, Close. Scheduling: serial.
- [ ] **T010 - CommanderDecisionGateWorker: treat equivalent resource delivery as a user decision, not default.** Worker: CommanderDecisionGateWorker. Read-only: yes. Explicit approval required: yes before selecting non-image resource delivery. Allowed files/resources: decision note defining resource digest/hash identity, distribution path, rollback path, and fresh-instance materialization proof. Forbidden actions: assuming resource delivery is equivalent without Commander/user decision, using manual pod patch, hiding persistence risk, K8S/Secret/ConfigMap mutation, `passes:true`, Close. Scheduling: serial.
- [ ] **T011 - CommanderDecisionGateWorker: decide local k3d registry vs persistent registry/resource path.** Worker: CommanderDecisionGateWorker. Read-only: yes. Explicit approval required: yes before any registry/resource mutation. Allowed files/resources: decision note comparing local k3d registry, persistent registry, and approved non-registry resource path. Forbidden actions: pushing to local or persistent registry, pulling images, changing image settings, changing deployment defaults, leaking registry credentials, `passes:true`, Close. Scheduling: serial.

## 4. HashManifestWorker Task

- [ ] **T012 - HashManifestWorker: define source artifact hash manifest.** Worker: HashManifestWorker. Read-only: yes until implementation approval. Explicit approval required: no for defining expected manifest; yes before reading/writing future source artifact files outside this tasks draft. Allowed files/resources: `spec.md`, `plan.md`, historical hash tables, future source artifact path only after approval. Forbidden actions: creating or modifying artifacts, image build/pull, runtime/K8S/registry/database mutation, `passes:true`, Close. Scheduling: serial after delivery decision; manifest design is parallel-safe with T016/T017 if read-only.
- [ ] **T013 - HashManifestWorker: require built artifact hash extraction.** Worker: HashManifestWorker. Read-only: yes until implementation approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION for any image build/pull/save/inspect or resource assembly. Allowed files/resources after approval: built image/resource artifact read-only extraction for the four allowlist files. Forbidden actions: extracting or modifying unrelated files, changing image tags, pushing/pulling without approval, broad OpenClaw -> GTClaw replacement, `passes:true`, Close. Scheduling: serial after T012 and implementation approval.
- [ ] **T014 - HashManifestWorker: require fresh deployed pod hash extraction.** Worker: HashManifestWorker. Read-only: yes after approved fresh instance exists. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION for fresh disposable instance creation and any pod/container read access. Allowed files/resources after approval: fresh deployed runtime pod metadata and the four allowlist file hashes at the proven runtime-served path. Forbidden actions: manual pod patch, `kubectl cp`, runtime file writes, restart, K8S mutation outside approved fresh-instance flow, `passes:true`, Close. Scheduling: serial after T013 and approved fresh instance creation.
- [ ] **T015 - HashManifestWorker: enforce three-way hash alignment and drift stop.** Worker: HashManifestWorker. Read-only: yes for comparison. Explicit approval required: no for comparison; yes for any follow-up artifact or runtime action. Allowed files/resources: manifest comparing source artifact, built artifact, and fresh deployed pod. Forbidden actions: continuing if before hash drift appears without Commander review, editing hashes to match, changing allowlist scope, `passes:true`, Close. Scheduling: serial gate.

Expected after-hash allowlist:

| File | Expected after SHA-256 | Expected size |
| --- | --- | ---: |
| `index.html` | `b26c425c6fdb6295779765ca8f3c90b661d5953b54a63a40ebe8e8ddeb1abcec` | `3398` |
| `assets/i18n-B06L7jQN.js` | `3c025ee187a558f73375360bd67c7ffac7e6a9a403847d983aeb46065d282b63` | `42617` |
| `assets/zh-CN-B26mMdbY.js` | `37337fecb0197581f7985ff2a004c60416aa4731315c8f7fa0e38dfc43c68809` | `23255` |
| `assets/index-M4TNVXB3.js` | `1dab28c0ea24a293aa511612eca0e7c8998ddbb556ec19a1ef6396ff5201b299` | `707959` |

Before-hash drift rule: if the selected base artifact does not show the known before SHA-256/size for these four files, stop for Commander review before any implementation continues. Drift may mean base image drift, wrong registry content, or wrong runtime-served path.

## 5. ImageDeliveryWorker Implementation Tasks

- [ ] **T016 - ImageDeliveryWorker: prepare implementation approval packet.** Worker: ImageDeliveryWorker. Read-only: yes. Explicit approval required: no to draft the packet; yes before any implementation. Allowed files/resources: `spec.md`, `plan.md`, `tasks.md`, PathVerificationWorker evidence, HashManifestWorker design. Forbidden actions: implementation, image build/tag/push/pull, registry mutation, runtime/K8S/database mutation, `passes:true`, Close. Scheduling: serial after T008-T015.
- [ ] **T017 - ImageDeliveryWorker: assemble four-file delta only.** Worker: ImageDeliveryWorker. Read-only: no after approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION. Allowed files/resources after approval: approved source artifact directory containing only `index.html`, `assets/i18n-B06L7jQN.js`, `assets/zh-CN-B26mMdbY.js`, `assets/index-M4TNVXB3.js`; proven runtime-served copy target. Forbidden actions: modifying backend/frontend/deployments/docs/longterm/AgentTeam/old evidence, changing runtime package identity, broad OpenClaw -> GTClaw replacement, adding files outside the four-file allowlist, outputting secrets, `passes:true`, Close. Scheduling: serial after T016.
- [ ] **T018 - ImageDeliveryWorker: build digest-pinned runtime image.** Worker: ImageDeliveryWorker. Read-only: no after approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION. Allowed files/resources after approval: approved build context, digest-pinned OpenClaw Image base, four allowlist files, local builder cache. Forbidden actions: build from mutable tag alone, app image build, deployment manifest edits, pulling without approval, pushing without separate approval, rewriting OpenClaw technical identifiers, `passes:true`, Close. Scheduling: serial after T017.
- [ ] **T019 - ImageDeliveryWorker: tag image with human-readable label plus immutable digest evidence.** Worker: ImageDeliveryWorker. Read-only: no after approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION. Allowed files/resources after approval: approved runtime image tag namespace and digest metadata. Forbidden actions: overwriting `latest`, tag-only rollback identity, pushing to registry without separate approval, K8S mutation, `passes:true`, Close. Scheduling: serial after T018.
- [ ] **T020 - ImageDeliveryWorker: push or publish runtime image/resource.** Worker: ImageDeliveryWorker. Read-only: no after approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION. Allowed files/resources after approval: approved local k3d registry, persistent registry, or approved resource path selected in T011. Forbidden actions: publishing to an unapproved registry/resource path, exposing credentials, changing deployment defaults, database mutation, `passes:true`, Close. Scheduling: serial after T019.
- [ ] **T021 - ImageDeliveryWorker: pull/inspect published artifact for digest verification.** Worker: ImageDeliveryWorker. Read-only: yes for inspect after approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION for pull/inspect against registry or resource location. Allowed files/resources after approval: published artifact digest, image index digest, linux/arm64 platform digest, four allowlist file hashes. Forbidden actions: mutating registry, retagging, changing runtime image settings, `passes:true`, Close. Scheduling: serial after T020.

## 6. Fresh Instance / Environment Mutation Tasks

- [ ] **T022 - TestEnvironmentMutationWorker: prepare fresh disposable instance approval packet.** Worker: TestEnvironmentMutationWorker. Read-only: yes. Explicit approval required: no to draft; yes before environment mutation. Allowed files/resources: decision record, approved artifact digest, rollback target, secret-redacted access plan. Forbidden actions: creating fresh instance, changing system image setting, patching Kubernetes resources, database mutation, `passes:true`, Close. Scheduling: serial after T021.
- [ ] **T023 - TestEnvironmentMutationWorker: set runtime image/resource for fresh disposable instance.** Worker: TestEnvironmentMutationWorker. Read-only: no after approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION. Allowed files/resources after approval: approved GTManager system setting or approved runtime resource reference needed for a disposable test instance only. Forbidden actions: deployment manifest default change, broad K8S mutation, Secret/ConfigMap mutation unless separately approved, manual pod patch as persistence evidence, `passes:true`, Close. Scheduling: serial after T022.
- [ ] **T024 - TestEnvironmentMutationWorker: create fresh disposable runtime instance.** Worker: TestEnvironmentMutationWorker. Read-only: no after approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION. Allowed files/resources after approval: a new disposable instance using the approved persistent artifact; runtime namespace/pod metadata for evidence. Forbidden actions: reusing the old T8I pod as fresh proof, patching pod files, exec-based file repair, deleting unrelated runtime pods, outputting token-bearing URLs, `passes:true`, Close. Scheduling: serial after T023.
- [ ] **T025 - TestEnvironmentMutationWorker: restore any temporary image/resource setting.** Worker: TestEnvironmentMutationWorker. Read-only: no after approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION if the setting change itself required approval. Allowed files/resources after approval: restore only the exact previous system image/resource reference recorded before T023. Forbidden actions: leaving undocumented environment drift, changing deployment defaults, deleting unrelated resources, leaking secrets, `passes:true`, Close. Scheduling: serial after T024 metadata capture.
- [ ] **T026 - TestEnvironmentMutationWorker: enforce no manual pod patch rule.** Worker: TestEnvironmentMutationWorker. Read-only: yes for review, no only if stopping approved mutation. Explicit approval required: no to stop and report. Allowed files/resources: environment mutation log and evidence notes. Forbidden actions: using manual pod patch, `kubectl cp`, in-pod file replacement, process restart, or shell repair as persistence evidence; `passes:true`; Close. Scheduling: serial gate.

## 7. EvidenceWorker Tasks

- [ ] **T027 - EvidenceWorker: record artifact identity evidence.** Worker: EvidenceWorker. Read-only: yes after approved implementation. Explicit approval required: yes if evidence collection requires registry/resource access. Allowed files/resources: base digest, new digest, platform digest, build command, tag/resource identity, timestamp, and builder context with secrets redacted. Forbidden actions: rebuilding, retagging, pushing, pulling without approval, outputting secrets, `passes:true`, Close. Scheduling: serial after T021; parallel-safe with T032/T033 only if all inputs already exist.
- [ ] **T028 - EvidenceWorker: record deployed runtime identity.** Worker: EvidenceWorker. Read-only: yes after approved fresh instance exists. Explicit approval required: yes for pod/container read access if required by Commander gate. Allowed files/resources: fresh instance namespace/name, pod image, imageID, platform digest, restart count, `openclaw` package version, runtime-served path. Forbidden actions: pod exec mutation, manual pod patch, restart, K8S mutation outside approved evidence read, token/cookie/secret output, `passes:true`, Close. Scheduling: serial after T024.
- [ ] **T029 - EvidenceWorker: record hash manifest evidence.** Worker: EvidenceWorker. Read-only: yes after approved artifact and fresh instance exist. Explicit approval required: yes for any artifact/pod read that is not already approved. Allowed files/resources: source artifact hashes, built artifact hashes, fresh deployed pod hashes for the four allowlist files. Forbidden actions: editing hashes, expanding allowlist, changing artifacts, runtime file writes, `passes:true`, Close. Scheduling: serial after T014/T015.
- [ ] **T030 - EvidenceWorker: run authorized GTManager control-ui browser DOM checks.** Worker: EvidenceWorker. Read-only: yes from browser perspective. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION for authorized browser E2E. Allowed files/resources after approval: redacted route metadata and DOM results for `/control-ui/`, `/control-ui/chat?session=main`, and history fallback; route cookies may be used but values must not be recorded. Forbidden actions: outputting token-bearing URLs, cookies, tokens, credentials, secrets; mutating runtime/K8S/database; `passes:true`; Close. Scheduling: serial after fresh deployed hash alignment.
- [ ] **T031 - EvidenceWorker: run `/proxy/` desktop regression evidence.** Worker: EvidenceWorker. Read-only: yes from browser perspective. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION for authorized browser E2E. Allowed files/resources after approval: redacted browser DOM/asset evidence for desktop access and `/proxy/`. Forbidden actions: changing proxy routes, mutating backend/frontend/runtime/K8S, recording token/cookie/secret data, `passes:true`, Close. Scheduling: serial after T030 or parallel-safe with T030 only if Commander approves same E2E session.
- [ ] **T032 - EvidenceWorker: run protected OpenClaw literal scan.** Worker: EvidenceWorker. Read-only: yes. Explicit approval required: yes if scan needs artifact/pod access not already approved. Allowed files/resources: built artifact and fresh deployed runtime control-ui files; protected categories `openclaw`, `OpenClaw Image`, `.openclaw`, `openclaw.json`, `openclaw dashboard --no-open`, `dist/control-ui`, package identity, paths, protocols, runtime/image/API/DB/K8S identifiers. Forbidden actions: broad OpenClaw -> GTClaw replacement, treating ambiguous literals as user-visible without proof, outputting secrets, `passes:true`, Close. Scheduling: parallel-safe with T027 if inputs exist.
- [ ] **T033 - EvidenceWorker: record rollback proof.** Worker: EvidenceWorker. Read-only: yes unless later rollback drill is separately approved. Explicit approval required: yes before any rollback drill or environment mutation. Allowed files/resources: prior image/resource digest, prior file hash set, operator rollback steps, and secret-free verification plan. Forbidden actions: mutable tag-only rollback, manual pod patch as only rollback path, exposing credentials, changing deployment defaults, `passes:true`, Close. Scheduling: serial before ReviewWorker.
- [ ] **T034 - EvidenceWorker: record lint status and waiver state.** Worker: EvidenceWorker. Read-only: yes. Explicit approval required: no for lint status command if Commander allows local check; yes before adding any waiver. Allowed files/resources: frontend lint command result summary and explicit waiver note if user later grants it. Forbidden actions: frontend code edits, silently waiving lint failure, claiming lint is clean without evidence, `passes:true`, Close. Scheduling: parallel-safe with T032/T033 if no code mutation.
- [ ] **T035 - EvidenceWorker: perform secret hygiene review.** Worker: EvidenceWorker. Read-only: yes. Explicit approval required: no for reviewing evidence draft; yes before accessing any secret-bearing system. Allowed files/resources: evidence packet drafts and command logs with redaction. Forbidden actions: printing token/cookie/secret values, writing token-bearing URLs, including `.env`, `.codex/auth.json`, `.codex/config.toml`, full environment dumps, `passes:true`, Close. Scheduling: serial final evidence gate.

## 8. Review / Close Gate Tasks

- [ ] **T036 - ReviewWorker: review scope delta before any status write-back.** Worker: ReviewWorker. Read-only: yes. Explicit approval required: no for review. Allowed files/resources: `git status --short`, path-limited diffs, evidence packet, spec/plan/tasks. Forbidden actions: changing files during review, hiding unrelated dirty worktree entries, staging/committing, `passes:true`, Close. Scheduling: serial after EvidenceWorker packet.
- [ ] **T037 - ReviewWorker: review evidence against FR-001 through FR-011 and SC-001 through SC-010.** Worker: ReviewWorker. Read-only: yes. Explicit approval required: no for review. Allowed files/resources: evidence packet, artifact digest data, hash manifest, browser DOM notes, rollback proof, lint/waiver state, secret hygiene review. Forbidden actions: using old T8I/addendum as persistence proof, overlooking missing fresh-instance evidence, hiding persistent risk, `passes:true`, Close. Scheduling: serial after T036.
- [ ] **T038 - ReviewWorker: require Commander review before any `passes:true`.** Worker: ReviewWorker. Read-only: yes. Explicit approval required: explicit Commander/user approval required before any later state write-back. Allowed files/resources: review memo only. Forbidden actions: writing `longterm/workspace/feature_list.json`, writing `claude-progress.txt`, changing specs/evidence, `passes:true`, Close. Scheduling: serial gate.
- [ ] **T039 - ReviewWorker: require explicit lint waiver if lint still exits nonzero.** Worker: ReviewWorker. Read-only: yes. Explicit approval required: yes for any waiver. Allowed files/resources: lint status evidence and waiver decision note. Forbidden actions: silently waiving lint, claiming lint status without command evidence, code cleanup outside this feature, `passes:true`, Close. Scheduling: serial gate.
- [ ] **T040 - ReviewWorker: require explicit Close/write-back approval after fresh E2E evidence.** Worker: ReviewWorker. Read-only: yes until future approval. Explicit approval required: REQUIRES EXPLICIT USER APPROVAL BEFORE EXECUTION for any Close/write-back step. Allowed files/resources after approval only: `longterm/workspace/feature_list.json`, `longterm/workspace/claude-progress.txt`, and final evidence references if Commander authorizes them. Forbidden actions: Close before evidence review, `passes:true` before Commander/user approval, hiding persistent risk, writing old evidence, changing backend/frontend/deployments/docs/AgentTeam, leaking secrets. Scheduling: final serial gate only.

## Verification Commands For This Tasks Draft

- [ ] **T041 - TasksDraftVerificationWorker: run whitespace validation.** Worker: TasksDraftVerificationWorker. Read-only: yes. Explicit approval required: no. Allowed files/resources: `specs/gtclaw-runtime-controlui-persistent-image/tasks.md`. Forbidden actions: implementation, environment mutation, `passes:true`, Close. Scheduling: serial.
  - Command: `git diff --check -- specs/gtclaw-runtime-controlui-persistent-image/tasks.md`
- [ ] **T042 - TasksDraftVerificationWorker: run required keyword scan.** Worker: TasksDraftVerificationWorker. Read-only: yes. Explicit approval required: no. Allowed files/resources: `specs/gtclaw-runtime-controlui-persistent-image/tasks.md`. Forbidden actions: implementation, environment mutation, `passes:true`, Close. Scheduling: serial after T041.
  - Command: `rg -n "PathVerificationWorker|HashManifestWorker|EvidenceWorker|REQUIRES EXPLICIT USER APPROVAL|/usr/local/lib/node_modules/openclaw/dist/control-ui|/opt/opensparrow/runtime/openclaw/dist/control-ui|passes:true|Close|manual pod patch|digest|rollback|secret" specs/gtclaw-runtime-controlui-persistent-image/tasks.md`
- [ ] **T043 - TasksDraftVerificationWorker: run path-limited status check.** Worker: TasksDraftVerificationWorker. Read-only: yes. Explicit approval required: no. Allowed files/resources: `specs/gtclaw-runtime-controlui-persistent-image/spec.md`, `specs/gtclaw-runtime-controlui-persistent-image/plan.md`, `specs/gtclaw-runtime-controlui-persistent-image/tasks.md`. Forbidden actions: staging, committing, reverting, implementation, environment mutation, `passes:true`, Close. Scheduling: serial after T042.
  - Command: `git status --short -- specs/gtclaw-runtime-controlui-persistent-image/spec.md specs/gtclaw-runtime-controlui-persistent-image/plan.md specs/gtclaw-runtime-controlui-persistent-image/tasks.md`
