# T8 E2E Evidence - gtmanager-gtclaw-m1-runtime-localization

Timestamp: 2026-04-30 23:56:11 Asia/Shanghai

## Evidence verdict: TEST ENV NOT READY

TEST ENV NOT READY - needs separate test-environment mutation prompt

T8 stopped at the required image-confirmation gate. The running runtime instance could not be confirmed as using the T6 dev image. No GTManager browser login, runtime proxy root, `/chat?session=main`, default zh-CN, wrapper, or runtime UI E2E checks were run after this stop condition.

## Environment and image confirmation

Expected T6 dev image tag:

`localhost:5001/clawmanager-openclaw/openclaw:gtclaw-resources-dev-20260430192029`

Expected T6 dev image ID:

`sha256:b5cef803f8eda8b3e0d3dc99d04147249a5c6354805501dfae2ae555484a4d45`

Expected base image digest:

`sha256:26bc8e3a16e45589927183a220b5683c7b3ab4563fca91d39af7fa4b64b11db8`

Read-only cluster observation found the running user runtime pod:

`clawmanager-user-1/clawreef-2-a1-local-registry-004259`

Observed running image:

`k3d-clawmanager-registry:5000/clawmanager-openclaw/openclaw:dev-arm64-pkt09-20260414170434`

Observed running image ID:

`k3d-clawmanager-registry:5000/clawmanager-openclaw/openclaw@sha256:26bc8e3a16e45589927183a220b5683c7b3ab4563fca91d39af7fa4b64b11db8`

Decision: stop T8. The running instance is the old/base image, not the T6 dev image.

## Commands/checks run with exit status, redacted

- `sed -n ... AGENTS.md` and required feature docs: exit 0.
- `git status --short`: exit 0. Dirty worktree existed before this evidence gate.
- `curl -sk -o /tmp/gtclaw-healthz-body.txt -w 'HTTP_STATUS=%{http_code}\n' https://localhost:30443/healthz`: exit 0, `HTTP_STATUS=200`.
- `wc -c /tmp/gtclaw-healthz-body.txt && sed -n '1,5p' /tmp/gtclaw-healthz-body.txt`: exit 0, body was `ok`.
- `kubectl get pods -A --field-selector=status.phase=Running ...`: exit 0, used only to inspect pod names, readiness, images, and image IDs.
- `kubectl get pods -n clawmanager-user-1 --field-selector=status.phase=Running ...`: exit 0, confirmed the only running user runtime pod image shown above.
- `mkdir -p specs/gtmanager-gtclaw-m1-runtime-localization/evidence`: exit 0, allowed evidence-path creation.

No token, secret, credential, `.env`, `.codex/auth.json`, or `.codex/config.toml` content was read or output.

## Wrapper evidence

Not collected. T8 stopped before browser login and wrapper route checks because the running runtime instance was not using the T6 dev image.

## Runtime proxy root evidence

Not collected. The proxy root check was intentionally not run after the image-confirmation stop condition.

## `/chat?session=main` independent evidence

Not collected. Because the running instance is not the T6 dev image, `/chat?session=main` was not tested and no root response is used as substitute evidence.

## Default zh-CN evidence

Not collected. Clean/default runtime profile rendering was not tested because the test environment is not on the required T6 dev image.

## GTClaw display evidence

Not collected. Runtime title, login gate, sidebar, breadcrumb, logo alt/title, chat welcome, and wrapper runtime surfaces were not tested because T8 stopped at image confirmation.

## Protected literal preservation evidence

Runtime protected-literal evidence was not collected from the running instance because the running instance is not the T6 dev image.

Protected literals remain required for the next evidence attempt:

- `openclaw`
- `OpenClaw Image`
- `clawmanager-openclaw-image`
- `.openclaw*`
- `openclaw.json`
- `openclaw dashboard --no-open`
- `dist/control-ui`
- `/usr/local/lib/node_modules/openclaw/dist/control-ui`
- API, DB, K8S, image tag, runtime status, storage, package, protocol, and path literals

## Patch/image hash evidence

T5/T6 after-hash input facts were not confirmed in a running runtime instance during this T8 attempt because the running pod is not the T6 dev image.

User-supplied T5/T6 after hashes for the T6 dev image are recorded here as input facts only:

| File | Expected after SHA-256 |
| --- | --- |
| `index.html` | `ed3560d9fa9b9156e62a405bc185c2d3495129ee3712ef8c536767f79d5778c7` |
| `assets/index-M4TNVXB3.js` | `eb8379dce913df535806486c8a4f637ab27f069c95925ef836821fbde2cb321b` |
| `assets/i18n-B06L7jQN.js` | `09c2150d93520982b375edbf1f59905169cbfe607d009c3434088ccb6894e406` |
| `assets/zh-CN-B26mMdbY.js` | `9a4ecc8992d00443ef59de0be41090099d5a1feb25cf062c5c02470044277f29` |

They are not current runtime E2E evidence until a running instance is read-only confirmed on the T6 dev image.

## Lint debt note

T7 risk note carried forward: full `npm run lint` remains pre-existing verification debt/final-gate risk, not passed.

## Remaining risks

- Test environment must be moved to, or a running test instance must be created with, the T6 dev image in a separate explicitly authorized test-environment mutation prompt.
- Runtime root and `/chat?session=main` remain untested for the T6 dev image.
- Default zh-CN rendering and GTClaw display surfaces remain unverified in real runtime UI.
- Protected OpenClaw technical literal preservation remains unverified in the running T6 runtime.
- Patch manifest after hashes remain input facts, not runtime evidence, until verified against a running T6 instance.

## Mutation statement

No source, deployment, docs, longterm, registry, pod, service, deployment, configmap, secret, instance image setting, or other K8S resource mutation was performed. Only this evidence packet was written under the user-approved evidence path. No Close was performed. No `passes:true` was written.
