# T8 E2E Evidence Rerun - gtmanager-gtclaw-m1-runtime-localization

Timestamp: 2026-05-01 Asia/Shanghai

## 1. Verdict

E2E EVIDENCE FAIL

The disposable test environment passed the required environment gate for instance `3`, but the E2E acceptance gate failed:

- GTManager proxy `/chat?session=main` returned `404 Not Found`.
- GTManager proxy root rendered a desktop shell titled `ClawManager Desktop`, not the patched `GTClaw 控制台` control-ui shell.
- Required GTManager wrapper routes still rendered GTManager as manager brand, but did not render GTClaw on the required runtime-facing surfaces.

No Close was performed and no `passes:true` was written.

## 2. Environment Gate Evidence

Required T8 rerun instance:

| Item | Observed value |
| --- | --- |
| Instance ID | `3` |
| Instance name | `gtclaw-t8-dev-20260501001159` |
| Pod | `clawmanager-user-1/clawreef-3-gtclaw-t8-dev-20260501001159` |
| Container | `desktop` |
| Pod phase | `Running` |
| Pod `Ready` | `true` |
| Container ready | `true` |
| Restart count | `0` |
| Image | `k3d-clawmanager-registry:5000/clawmanager-openclaw/openclaw:gtclaw-resources-dev-20260430192029` |
| ImageID | `k3d-clawmanager-registry:5000/clawmanager-openclaw/openclaw@sha256:b5cef803f8eda8b3e0d3dc99d04147249a5c6354805501dfae2ae555484a4d45` |
| Package | `openclaw@2026.4.14` |
| Control UI path | `/usr/local/lib/node_modules/openclaw/dist/control-ui` |

Environment decision: may proceed beyond the environment gate. The running pod image, imageID/index digest, package, path, and four after-hashes matched the required T6 dev identity.

## 3. Commands Run With Exit Status, Redacted

No command output contains credentials, cookies, tokens, token-bearing URLs, `.env`, `.codex/auth.json`, or `.codex/config.toml`.

| Command / check | Exit | Result |
| --- | ---: | --- |
| `search_memories` for project facts/session state | n/a | Found prior T8 `TEST ENV NOT READY` and T6 dev image facts |
| Read allowed `AGENTS.md`, constitution, backend/frontend `AGENTS.md`, current spec/plan/tasks, and two allowed evidence files | `0` | Context loaded |
| `git status --short` before evidence work | `0` | Dirty worktree existed before this gate |
| `curl -sk https://localhost:30443/healthz` | `0` | HTTP `200`, body `ok` |
| `kubectl get pod ... clawreef-3-gtclaw-t8-dev-20260501001159 ...` | `0` | Running/Ready, restarts `0`, expected image and imageID |
| `kubectl exec ... node -e package check` | `0` | `openclaw@2026.4.14` |
| `kubectl exec ... test -d /usr/local/lib/node_modules/openclaw/dist/control-ui` | `0` | Path exists |
| `kubectl exec ... wc -c <four allowlist files>` | `0` | After sizes recorded below |
| `kubectl exec ... sha256sum <four allowlist files>` | `0` | After hashes matched T6 |
| `cd frontend && npm run lint` | `1` | Known lint debt: `129 problems (110 errors, 19 warnings)` |
| `cd backend && go test ./...` | `0` | Backend tests passed |
| `kubectl apply --dry-run=client -f deployments/k3s/clawmanager.yaml` | `0` | Client dry-run succeeded |
| `kubectl apply --dry-run=client -f deployments/k8s/clawmanager.yaml` | `0` | Client dry-run succeeded |
| In-app browser bootstrap via Browser plugin | n/a | Blocked by JS REPL static builtin import limitation; fell back to local Chrome CDP |
| Authenticated `POST /api/v1/auth/login` | `0` | HTTP `200`; tokens kept only in process memory |
| Authenticated `POST /api/v1/instances/3/access` | `0` | HTTP `200`; access token/proxy URL kept only in process memory |
| GTManager route CDP inspection | `0` | Wrapper route evidence recorded below |
| Runtime proxy root CDP inspection | `0` | HTTP-rendered route title `ClawManager Desktop` |
| Runtime proxy `/chat?session=main` CDP inspection | `0` | Rendered `404 Not Found` |
| Direct in-pod control-ui probes on `127.0.0.1:18789` | `0` | Root and `/chat?session=main` returned `200` as supplementary non-proxy evidence |

## 4. GTManager Login And Wrapper Route Evidence

Login used the GTManager API authorization flow. Credentials and returned tokens were not written to evidence. Browser storage was populated only inside the temporary Chrome process.

| Route | Final path | Title | HTML lang | GTManager visible | GTClaw visible | Chinese visible | Evidence summary |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `/instances/new` | `/instances/new` | `GTManager` | `zh` | yes | no | yes | Create-instance step showed `GTManager` and Chinese form labels; no GTClaw string observed |
| `/portal` | `/portal` | `GTManager` | `zh` | yes | no | yes | Portal listed instances with `openclaw` type/status; no GTClaw string observed |
| `/openclaw-configs` | `/openclaw-configs` | `GTManager` | `zh` | yes | no | yes | Page still showed `OpenClaw 资源管理`; no GTClaw string observed |
| `/admin/settings` | `/admin/settings` | `GTManager` | `zh` | yes | no | yes | Runtime image cards still showed OpenClaw/openclaw text; no GTClaw string observed |
| `/admin/instances` | `/admin/instances` | `GTManager` | `zh` | yes | no | yes | Global instance list showed type `openclaw`; no GTClaw string observed |
| `/instances/3` | `/instances/3` | `GTManager` | `zh` | yes | no | yes | Instance detail showed `OPENCLAW` and technical lower-case runtime values; no GTClaw display string observed |
| `/admin/ai-audit` | `/admin/ai-audit` | `GTManager` | `zh` | yes | no | yes | AI audit route reachable; no GTClaw display string observed |
| `/admin/ai-gateway` | `/admin/ai-gateway` | `GTManager` | `zh` | yes | no | yes | AI gateway route reachable; no GTClaw display string observed |

Wrapper decision: FAIL. GTManager remains the manager brand, but required runtime-facing wrapper surfaces did not display GTClaw in the deployed app.

## 5. Runtime Proxy Root Evidence

Authenticated access was obtained with:

- `POST /api/v1/instances/3/access`: HTTP `200`
- Response keys observed: `access_url`, `expires_at`, `proxy_url`, `token`
- Token and token-bearing `proxy_url` were kept only in process memory and not written to evidence.

Proxy root rendered through Chrome/CDP:

| Field | Observed |
| --- | --- |
| Safe path | `/api/v1/instances/3/proxy/` |
| Final path | `/api/v1/instances/3/proxy/` |
| Title | `ClawManager Desktop` |
| HTML lang | `en` |
| Ready state | `complete` |
| Body chars | `119` |
| Asset hint | `assets/index-luFV63l_.js` |
| Contains GTClaw | no |
| Contains Chinese | no |

Proxy root decision: FAIL for the T8 runtime control-ui gate. The authenticated proxy root rendered a desktop shell, not the patched `GTClaw 控制台` control-ui shell from `/usr/local/lib/node_modules/openclaw/dist/control-ui`.

## 6. `/chat?session=main` Independent Evidence

Authenticated proxy chat route was tested independently from proxy root:

| Field | Observed |
| --- | --- |
| Safe path | `/api/v1/instances/3/proxy/chat` |
| Query | `session=main` present; token kept only in process memory |
| Final path | `/api/v1/instances/3/proxy/chat` |
| Title | `404 Not Found` |
| Ready state | `complete` |
| Body chars | `35` |
| Contains GTClaw | no |
| Contains Chinese | no |
| Contains 404 | yes |

`/chat?session=main` decision: FAIL. Proxy root success cannot substitute for this independent route.

Supplementary non-acceptance observation: inside the runtime pod, direct control-ui on `127.0.0.1:18789` returned HTTP `200` for both `/` and `/chat?session=main`. That proves the patched control-ui process is available inside the pod, but it does not satisfy the GTManager proxy E2E route requirement.

## 7. Default zh-CN Evidence

Proxy path:

- Proxy root contained no Chinese text.
- Proxy `/chat?session=main` returned `404`.

Supplementary direct control-ui path through a temporary local port-forward to pod localhost `18789`:

| Route | Title | Rendered path | Contains GTClaw | Contains `GTClaw 控制台` | Chinese visible | Example visible Chinese |
| --- | --- | --- | --- | --- | --- | --- |
| direct root | `GTClaw 控制台` | `/chat?session=main` | yes | yes | yes | `可开始对话` |
| direct `/chat?session=main` | `GTClaw 控制台` | `/chat?session=main` | yes | yes | yes | `可开始对话` |

Default zh-CN decision: FAIL for proxy E2E. Direct control-ui shows patched Chinese/GTClaw text, but GTManager proxy did not expose that route.

## 8. GTClaw Display Evidence

Runtime direct control-ui supplementary evidence:

- Browser title: `GTClaw 控制台`
- Sidebar/product text included `GTClaw`
- Logo `alt` attributes included `GTClaw`
- Chat/welcome area included Chinese text `可开始对话`
- `OpenClaw Control` was not observed in the direct rendered control-ui path

Runtime proxy evidence:

- Proxy root title was `ClawManager Desktop`
- Proxy root did not contain GTClaw or Chinese text
- Proxy `/chat?session=main` returned `404 Not Found`

GTManager wrapper evidence:

- Required wrapper routes were reachable and remained Chinese-default, with `GTManager` still visible as manager brand.
- Required runtime-facing wrapper routes did not display GTClaw in the deployed app.

GTClaw display decision: FAIL for E2E acceptance.

## 9. Protected Literal Preservation Evidence

Runtime allowlist scan in the running T6 dev pod:

| Literal | Count in `control-ui` allowlist files |
| --- | ---: |
| `openclaw` | 72 |
| `OpenClaw Image` | 0 |
| `clawmanager-openclaw-image` | 0 |
| `.openclaw` | 9 |
| `openclaw.json` | 3 |
| `openclaw dashboard --no-open` | 4 |
| `dist/control-ui` | 0 |
| `/usr/local/lib/node_modules/openclaw/dist/control-ui` | 0 |

Runtime display scan:

| Runtime display string | Count/location |
| --- | --- |
| `GTClaw` | 6 in `assets/index-M4TNVXB3.js` |
| `GTClaw 控制台` | 1 in `index.html`; 1 in `assets/zh-CN-B26mMdbY.js` |
| `OpenClaw` | 2 in `assets/index-M4TNVXB3.js` |
| `OpenClaw Control` | 0 |

Deployed GTManager bundle scan:

| Literal | Count in deployed manager bundle |
| --- | ---: |
| `openclaw` | 119 |
| `clawmanager-openclaw-image` | 1 |
| `.openclaw` | 60 |
| `OpenClaw Image` | 0 |
| `openclaw.json` | 0 |
| `openclaw dashboard --no-open` | 0 |
| `dist/control-ui` | 0 |
| `/usr/local/lib/node_modules/openclaw/dist/control-ui` | 0 |

Protected literal decision: no evidence of protected lower-case OpenClaw technical literals being removed from the scanned runtime allowlist or deployed manager bundle. This does not overcome the proxy/chat and wrapper display E2E failures.

## 10. Patch / Hash Evidence

Current running pod hashes matched the T6 after-hashes.

| File | Before size | Before SHA-256 | After size in running pod | After SHA-256 in running pod |
| --- | ---: | --- | ---: | --- |
| `index.html` | 3,395 | `f313071437a1b8c432024d3f83af4056fb672a4fe15b93be8b2291dcaac0115c` | 3,395 | `ed3560d9fa9b9156e62a405bc185c2d3495129ee3712ef8c536767f79d5778c7` |
| `assets/index-M4TNVXB3.js` | 707,545 | `e89d5e55d89aaae7bc64598b949335425df7626f57b12a8780426a1911315882` | 707,543 | `eb8379dce913df535806486c8a4f637ab27f069c95925ef836821fbde2cb321b` |
| `assets/i18n-B06L7jQN.js` | 42,702 | `09c2150d93520982b375edbf1f59905169cbfe607d009c3434088ccb6894e406` | 42,702 | `09c2150d93520982b375edbf1f59905169cbfe607d009c3434088ccb6894e406` |
| `assets/zh-CN-B26mMdbY.js` | 23,247 | `2afe4858d80c81247f01e21198011a78180de12e72f567b5606fe9355dbfd2c1` | 23,248 | `9a4ecc8992d00443ef59de0be41090099d5a1feb25cf062c5c02470044277f29` |

Image identity:

- Expected and observed image: `k3d-clawmanager-registry:5000/clawmanager-openclaw/openclaw:gtclaw-resources-dev-20260430192029`
- Expected and observed imageID/index digest: `sha256:b5cef803f8eda8b3e0d3dc99d04147249a5c6354805501dfae2ae555484a4d45`

## 11. Risks / Blockers

- Blocking: GTManager proxy `/chat?session=main` returns `404`.
- Blocking: GTManager proxy root exposes `ClawManager Desktop`/`assets/index-luFV63l_.js`, not the patched `GTClaw 控制台` control-ui shell.
- Blocking: deployed GTManager wrapper routes do not show GTClaw on required runtime-facing surfaces.
- Risk: direct control-ui on pod localhost `18789` appears patched, but that port is not exposed by the current GTManager instance proxy service path used by T8.
- Risk: `npm run lint` still fails with the known pre-existing lint debt (`129 problems`, `110 errors`, `19 warnings`), which remains a final Close/pass-style blocker unless separately fixed or explicitly accepted as existing debt.

## 12. Mutation Statement

This T8 rerun performed evidence collection only.

- No `frontend/**`, `backend/**`, `deployments/**`, `docs/**`, `longterm/**`, `spec.md`, `plan.md`, `tasks.md`, or old M1 feature files were modified.
- No pod, service, deployment, system image setting, registry, Kubernetes resource, or source/deployment/longterm mutation was performed.
- A temporary local Chrome process and a temporary local `kubectl port-forward` were used only for observation and were not Kubernetes resource mutations.
- No credential, cookie, token, secret, `.env`, `.codex/auth.json`, or `.codex/config.toml` content was written to evidence.
- No Close was performed.
- No `passes:true` was written.
