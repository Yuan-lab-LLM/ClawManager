# GTManager M1 Evidence Packet - 2026-04-30

## Verdict

Final evidence verdict: **E2E blocked**.

Do not use this packet as approval for write-back or a durable feature pass flag. The deployed UI at `https://localhost:30443` is reachable, but the clean-profile login page still presents `ClawManager`, `html lang="en"`, selected language `English`, and favicon `/lobster_transparent.png`. GTManager branding and Chinese default behavior are therefore not proven in the deployed E2E target.

## Review Context

- User-provided ReviewWorker conclusion: Critical=None, Important=None, Minor=None.
- ReviewWorker allowed moving into Evidence/E2E.
- ReviewWorker conclusion for lint: targeted 7 lint errors are pre-existing debt.
- Assets worker C remains BLOCKED because exact user-provided logo, favicon, and loading asset source paths are missing.
- Write-back is not authorized.
- `longterm/workspace/feature_list.json` was not updated.

## Environment

Reference log: `logs/environment.log`

- Repo: `/Users/eduardogan/Desktop/GHJProject/ClawManager`
- Date: 2026-04-30 UTC command evidence; local environment timezone from session: Asia/Shanghai
- OS: Darwin arm64
- Node: `v23.11.0`
- npm: `10.9.2`
- Go: `go1.25.0 darwin/arm64`
- kubectl client: `v1.32.2`
- kubectl context: `k3d-clawmanager`
- E2E base URL: `https://localhost:30443`

## Commands Run

| Command | Exit code | Evidence |
| --- | ---: | --- |
| `cd frontend && npm run build` | 0 | `logs/frontend-npm-run-build.log` |
| `cd frontend && npm run lint` | 1 | `logs/frontend-npm-run-lint.log` |
| `cd backend && go test ./...` | 0 | `logs/backend-go-test.log` |
| `kubectl apply --dry-run=client -f deployments/k3s/clawmanager.yaml` | 0 | `logs/kubectl-dry-run-k3s.log` |
| `kubectl apply --dry-run=client -f deployments/k8s/clawmanager.yaml` | 0 | `logs/kubectl-dry-run-k8s.log` |
| `curl -sk https://localhost:30443/healthz` | 0 | `logs/curl-healthz.log` |
| `curl -skI https://localhost:30443/login` and login HTML probe | 0 | `logs/curl-login-head.log` |
| Admin API login probe with default local admin credentials, token values redacted | 0 | `logs/curl-admin-login-probe.log` |
| Playwright E2E probe | 0 wrapper/script exit; verdict blocked | `logs/playwright-e2e-probe.log`, `playwright-result.json` |

Build note: target-repo writes were limited to this evidence directory. Because `npm run build` writes `frontend/dist` and TypeScript build info under `node_modules/.tmp`, it was run in a temporary copy of `frontend/` to respect the no-write boundary for `frontend/**` and `dist/**`. The temporary directory was removed after logging.

Lint status: `npm run lint` exited 1. The log reports `135 problems (115 errors, 20 warnings)`. Per ReviewWorker, the targeted 7 lint errors are pre-existing debt. This packet does not treat lint as an M1 success condition.

## Playwright E2E Coverage

Tooling note: the in-app browser runtime was attempted first, but the available `js_repl` could not import the browser-client module because static builtin imports in that module are unsupported by this REPL. Local Playwright was used as fallback.

Playwright artifacts:

- Result JSON: `playwright-result.json`
- Screenshots: `screenshots/`
- Script: `e2e-playwright-probe.mjs`

Clean-profile `/login`:

- URL: `https://localhost:30443/login`
- Browser title: `ClawManager`
- HTML language: `en`
- Selected language: `English`
- GTManager visible/title signal: false
- ClawManager visible/title signal: true
- Favicon href: `/lobster_transparent.png`
- Favicon request: HTTP 200
- Screenshot: `screenshots/login-clean-profile.png`
- Result: blocked; default Chinese and GTManager brand are not shown.

Admin shell routes:

- `/admin` -> screenshot `screenshots/admin-dashboard.png`
- `/admin/users` -> screenshot `screenshots/admin-users.png`
- `/admin/instances` -> screenshot `screenshots/admin-instances.png`
- `/admin/ai-gateway` -> screenshot `screenshots/admin-ai-gateway.png`
- `/admin/security` -> screenshot `screenshots/admin-security-center.png`
- `/admin/settings` -> screenshot `screenshots/admin-settings.png`
- Route access note: UI login stayed on a loading screen, so route coverage used same-origin API token bootstrap with default local admin credentials. Tokens were not written to logs.
- Result: routes were reachable, but they showed `ClawManager` branding and mostly English UI.

User shell routes:

- `/dashboard` -> screenshot `screenshots/user-dashboard.png`
- `/instances` -> screenshot `screenshots/user-instances.png`
- `/openclaw-configs` -> screenshot `screenshots/user-openclaw-resource-management.png`
- `/settings` -> screenshot `screenshots/user-settings.png`
- `/portal` -> screenshot `screenshots/user-instance-access-surface.png`
- Credential note: no separate regular-user credential was provided. These routes were observed under the authenticated admin context because the app permits authenticated access to those protected routes.
- Result: routes were reachable, but they showed `ClawManager` branding and mostly English UI.

OpenClaw runtime wording:

- `OpenClaw Resource Management` was still visible on `/openclaw-configs`.
- `OpenClaw Desktop` / image wording was visible in admin settings.
- Result: OpenClaw runtime wording remained observable where runtime identity is named.

Health/API/protected identifiers:

- `/healthz` returned `ok` with exit code 0.
- `/api/v1/auth/login` returned HTTP 200 for default local admin credentials; token values were redacted in logs.
- K3S/K8S dry-run logs show protected Kubernetes identifiers such as `clawmanager-system`, `clawmanager-app`, `clawmanager-frontend`, `clawmanager-gateway`, and `clawmanager-egress-proxy`.
- Protected identifiers were not migrated by this verifier.

## Blockers

- Deployed frontend at `https://localhost:30443` does not reflect the required GTManager/Chinese-default M1 behavior. Curl HTML also shows `last-modified: Thu, 16 Apr 2026`, `<html lang="en">`, `<title>ClawManager</title>`, and favicon `/lobster_transparent.png`.
- Clean-profile Playwright evidence shows login defaults to English and displays `ClawManager`, not GTManager.
- UI login did not settle into authenticated route coverage; route coverage required API token bootstrap.
- No separate regular-user credentials were supplied for true regular-user coverage.
- Asset task C remains BLOCKED: exact logo/favicon/loading source paths are missing, so final asset replacement cannot be verified.
- Lint remains exit 1 and is recorded as pre-existing debt per ReviewWorker, not as M1 acceptance evidence.

## Manual Retest Request Packet

Use this only after the deployed target is refreshed to the candidate M1 frontend and any required user credentials are available. Manual feedback is valid E2E evidence only if the tester records actual observations and screenshots.

Environment to record:

| Field | Tester input |
| --- | --- |
| Tester | |
| Date/time | |
| Browser and version | |
| Target URL | |
| Deployment/build identifier | |
| Admin credential source | |
| Regular-user credential source, if available | |

Steps:

1. Open a clean browser profile with no stored `clawmanager_locale` and navigate to `https://localhost:30443/login`.
2. Record browser title, selected language, visible product name, favicon request, and screenshot.
3. Log in as admin and visit `/admin`, `/admin/users`, `/admin/instances`, `/admin/ai-gateway`, `/admin/security`, and `/admin/settings`.
4. Record whether each admin route shows GTManager manager branding and Chinese default UI.
5. Log in as a regular user if credentials exist and visit `/dashboard`, `/instances`, `/openclaw-configs`, `/settings`, and instance access surfaces.
6. Record whether OpenClaw runtime wording remains OpenClaw on resource/runtime surfaces.
7. Confirm `https://localhost:30443/healthz` returns `ok`.
8. Record any remaining `ClawManager` visible text and classify whether it is product-facing or a protected technical/historical identifier.

Feedback table:

| Area | Expected | Actual observation | Screenshot/reference | Result |
| --- | --- | --- | --- | --- |
| Clean `/login` default locale | Chinese default | | | |
| Clean `/login` brand | GTManager | | | |
| Browser title | GTManager | | | |
| Favicon | Approved GTManager asset, or blocked if C still blocked | | | |
| Admin dashboard | Chinese + GTManager | | | |
| Admin users | Chinese + GTManager | | | |
| Admin instances | Chinese + GTManager | | | |
| AI Gateway | Chinese + GTManager | | | |
| Security center | Chinese + GTManager | | | |
| Admin settings | Chinese + GTManager, OpenClaw where runtime is named | | | |
| User workspace | Chinese + GTManager | | | |
| User instances | Chinese + GTManager | | | |
| OpenClaw resources | Chinese + GTManager shell, OpenClaw runtime wording preserved | | | |
| User settings | Chinese + GTManager | | | |
| Instance access surfaces | Chinese + GTManager shell, OpenClaw runtime wording preserved | | | |
| `/healthz` | `ok` | | | |

## Final Gate State

Final verdict: **E2E blocked**.

Next gate: refresh or redeploy the E2E target so `https://localhost:30443` serves the M1 candidate UI, provide any needed regular-user credentials, keep asset task C blocked unless exact paths are supplied, then rerun Playwright or return filled manual feedback using the packet above.
