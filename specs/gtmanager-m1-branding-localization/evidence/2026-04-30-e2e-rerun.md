# GTManager M1 E2E Rerun Evidence Packet - 2026-04-30

## Verdict

Final evidence verdict: **E2E partial / blocked**.

The refreshed local K3S deployment now serves the GTManager/Chinese-default bundle and Playwright validated the clean-profile `/login` text/locale checks plus all requested admin and user route surfaces. However, UI login still remains on a `加载中...` loading surface after submit; this packet records that issue and used same-origin API token bootstrap only to continue route coverage. This packet does **not** authorize Close/write-back and does **not** write any durable pass flag.

## Environment

Reference log: `rerun-2026-04-30/logs/environment.log`

- Repo: `/Users/eduardogan/Desktop/GHJProject/ClawManager`
- Evidence root: `specs/gtmanager-m1-branding-localization/evidence/rerun-2026-04-30/`
- E2E base URL: `https://localhost:30443`
- Local date: 2026-04-30
- Recorded UTC window: 2026-04-30T03:00:24Z to 2026-04-30T03:01:47Z
- kubectl context: `k3d-clawmanager`
- Deployment image observed: `clawmanager:m1-gtmanager-local-20260430102258`
- Playwright browser: Chromium, headless, clean context, `locale: en-US`

## Commands Run

Full command status log: `rerun-2026-04-30/logs/command-status.tsv`

| Command | Exit | Evidence |
| --- | ---: | --- |
| Environment capture | 0 | `rerun-2026-04-30/logs/environment.log` |
| `curl -sk https://localhost:30443/healthz` | 0 | `rerun-2026-04-30/logs/prereq-curl-healthz.log` |
| `kubectl get deploy -n clawmanager-system clawmanager-app -o jsonpath='{.spec.template.spec.containers[?(@.name=="clawmanager-app")].image}{"\n"}'` | 0 | `rerun-2026-04-30/logs/prereq-kubectl-deploy-image.log` |
| `set -o pipefail; curl -sk https://localhost:30443/login \| head -40` | 0 | `rerun-2026-04-30/logs/prereq-curl-login-head40.log` |
| `curl -sk https://localhost:30443/login -o .../logs/login.html` | 0 | `rerun-2026-04-30/logs/login.html` |
| Extract deployed JS path from login HTML | 0 | `rerun-2026-04-30/logs/deployed-js-path.txt` |
| `curl -sk https://localhost:30443/assets/index-yeoXoo5V.js -o .../logs/deployed-app.js` | 0 | `rerun-2026-04-30/logs/deployed-app.js` |
| `grep -q 'GTManager' .../deployed-app.js` | 0 | `rerun-2026-04-30/logs/grep-q-GTManager.log` |
| `grep -q 'ClawManager' .../deployed-app.js` | 1 | `rerun-2026-04-30/logs/grep-q-ClawManager.log` |
| `grep -q 'clawmanager_locale' .../deployed-app.js` | 0 | `rerun-2026-04-30/logs/grep-q-clawmanager_locale.log` |
| `grep -q '登录 GTManager' .../deployed-app.js` | 0 | `rerun-2026-04-30/logs/grep-q-login-zh-GTManager.log` |
| `grep -q 'Sign in to GTManager' .../deployed-app.js` | 0 | `rerun-2026-04-30/logs/grep-q-signin-GTManager.log` |
| `grep -q 'Sign in to ClawManager' .../deployed-app.js` | 1 | `rerun-2026-04-30/logs/grep-q-signin-ClawManager.log` |
| `npm init -y && npm install --no-save playwright@1.59.1` in `/tmp` | 0 | `rerun-2026-04-30/logs/playwright-rerun.log` |
| Playwright E2E rerun against `https://localhost:30443` | 0 | `rerun-2026-04-30/logs/playwright-rerun.log`, `rerun-2026-04-30/playwright-rerun-result.json` |

Note: early `grep -n ... | head` excerpt commands are present in `command-status.tsv`; the authoritative presence/absence checks are the later `grep -q` rows above.

## Prerequisite Evidence

- `/healthz`: returned `ok`.
- Deployment image: `clawmanager:m1-gtmanager-local-20260430102258`.
- `/login` first 40 lines show `<html lang="zh">`, `<title>GTManager</title>`, JS `/assets/index-yeoXoo5V.js`, favicon `/lobster_transparent.png`.
- Deployed JS checks:
  - `GTManager`: present.
  - `ClawManager`: absent by exact uppercase product-facing grep.
  - `clawmanager_locale`: present as protected technical/localStorage key.
  - `登录 GTManager`: present.
  - `Sign in to GTManager`: present.
  - `Sign in to ClawManager`: absent.

Lint status is inherited from prior evidence: `npm run lint` exited 1 due pre-existing debt per ReviewWorker. This rerun did not re-run lint because the current authorization limited writes to the evidence directory and the user requested rerun evidence against the refreshed deployment.

## Playwright Result

- Script: `rerun-2026-04-30/e2e-playwright-rerun.mjs`
- Result JSON: `rerun-2026-04-30/playwright-rerun-result.json`
- Console log JSON: `rerun-2026-04-30/logs/playwright-console-events.json`
- Network log JSON: `rerun-2026-04-30/logs/playwright-network-events.json`
- Screenshots directory: `rerun-2026-04-30/screenshots/`
- Playwright script exit code: 0
- Playwright verdict: `E2E partial / blocked`

Summary from result JSON:

| Area | Covered | Passed | Notes |
| --- | ---: | ---: | --- |
| Clean `/login` | 1 | 1 | `lang=zh`, runtime document lang `zh`, title `GTManager`, selector `zh` / `中文`, visible `登录 GTManager`, no product-facing `ClawManager`. |
| Admin routes | 8 | 8 | Dashboard, users, instances, AI Gateway, security center, reports, scanner config, settings. |
| User routes | 5 | 5 | Dashboard/workspace, instances, OpenClaw Resource Management, settings, portal/instance access surface. Covered under admin authenticated context because no separate regular-user credentials were provided. |
| OpenClaw runtime wording | 2 surfaces | 2 | Observed on `/admin/settings` and `/openclaw-configs`; OpenClaw runtime naming remains OpenClaw. |
| Protected identifiers | observed | observed | `/healthz`, `/api/v1/auth/login`, `clawmanager_locale`, `clawmanager-system`, `clawmanager-app`, and lower-case technical image/user identifiers remain observable. |

## Clean Profile Login

Screenshot: `rerun-2026-04-30/screenshots/login-clean-profile.png`

- URL: `https://localhost:30443/login`
- Browser title: `GTManager`
- HTML lang: `zh`
- Runtime `document.documentElement.lang`: `zh`
- Language selector value/text: `zh` / `中文`
- Visible body excerpt: `语言 English 中文 日本語 한국어 Deutsch 登录 GTManager 虚拟桌面管理平台 用户名 密码 登录`
- Product-facing `ClawManager`: not visible.
- Favicon href: `/lobster_transparent.png`
- Favicon request: recorded in Playwright network log; HTTP 200.
- Asset classification: still lobster, so Assets worker C remains **BLOCKED** due missing exact logo/favicon/loading paths; this is not counted as an M1 text/localization failure.

## UI Login Issue

Screenshot: `rerun-2026-04-30/screenshots/after-admin-login.png`

- UI login was attempted with default local admin credentials; password was not written to logs.
- After submit, the page remained at `/login` with body excerpt `加载中...`.
- Playwright recorded this issue as: `UI login did not complete and remained on a Loading surface.`
- API token bootstrap was then used only to continue route coverage.
- `/api/v1/auth/login` returned HTTP 200 and usable tokens; token values were not written to logs.

## Admin Route Coverage

All admin routes below rendered Chinese UI, visible GTManager branding, title `GTManager`, `html lang=zh`, no product-facing `ClawManager`, and no redirect to login after API token bootstrap.

| Route | Screenshot | Result |
| --- | --- | --- |
| `/admin` | `rerun-2026-04-30/screenshots/admin-dashboard.png` | passed |
| `/admin/users` | `rerun-2026-04-30/screenshots/admin-users.png` | passed |
| `/admin/instances` | `rerun-2026-04-30/screenshots/admin-instances.png` | passed |
| `/admin/ai-gateway` | `rerun-2026-04-30/screenshots/admin-ai-gateway.png` | passed |
| `/admin/security` | `rerun-2026-04-30/screenshots/admin-security-center.png` | passed |
| `/admin/security/reports` | `rerun-2026-04-30/screenshots/admin-security-reports.png` | passed |
| `/admin/security/scanner` | `rerun-2026-04-30/screenshots/admin-security-scanner.png` | passed |
| `/admin/settings` | `rerun-2026-04-30/screenshots/admin-settings.png` | passed |

## User Route Coverage

No separate regular-user credentials were provided. The surfaces below were covered under the authenticated admin context because the app permits access to these protected routes after authentication. Each route rendered Chinese UI, visible GTManager branding, title `GTManager`, `html lang=zh`, no product-facing `ClawManager`, and no redirect to login.

| Route | Screenshot | Result |
| --- | --- | --- |
| `/dashboard` | `rerun-2026-04-30/screenshots/user-dashboard.png` | passed |
| `/instances` | `rerun-2026-04-30/screenshots/user-instances.png` | passed |
| `/openclaw-configs` | `rerun-2026-04-30/screenshots/user-openclaw-resource-management.png` | passed |
| `/settings` | `rerun-2026-04-30/screenshots/user-settings.png` | passed |
| `/portal` | `rerun-2026-04-30/screenshots/user-instance-access-surface.png` | passed |

## Runtime and Identifier Boundary

- OpenClaw runtime wording remains observable on runtime/resource surfaces:
  - `/openclaw-configs`: `OpenClaw 资源管理`
  - `/admin/settings`: `OpenClaw Desktop`, `clawmanager-openclaw-image/openclaw:latest`
- Protected technical identifiers remain observable and were not treated as product-facing rename failures:
  - Kubernetes: `clawmanager-system`, `clawmanager-app`
  - API: `/healthz`, `/api/v1/auth/login`
  - Locale storage key: `clawmanager_locale`
  - Technical image/user identifiers such as `clawmanager-openclaw-image` and `clawmanager-user-1`

## Blockers and Issues

- **Blocking issue for E2E pass/Close**: UI login still remains on a `加载中...` loading surface after submit. Route coverage continued only through API token bootstrap.
- **Asset task C remains BLOCKED**: exact logo, favicon, and loading source paths are still missing. Favicon is still `/lobster_transparent.png` and returns HTTP 200.
- **Separate regular-user credential unavailable**: user route surfaces were covered under admin authenticated context, not a distinct regular-user login.
- **Lint inherited status**: lint exit 1 due pre-existing debt per ReviewWorker.
- Close/write-back remains unauthorized.
- No durable pass flag was written.

## Final Gate State

Final evidence verdict: **E2E partial / blocked**.

Close approval gate should **not** be considered ready from this packet alone because UI login is still blocked on the loading surface and Assets C remains blocked pending exact source paths. A later Close/write-back step would still require explicit user approval and must not write a durable pass flag unless the user authorizes it after acceptable E2E evidence exists.
