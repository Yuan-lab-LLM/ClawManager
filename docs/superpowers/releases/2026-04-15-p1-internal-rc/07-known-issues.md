# Known Issues

| Type | Issue | Evidence Path | Impact |
| --- | --- | --- | --- |
| Gate 1 boundary | fresh repo-default Mac/ARM deploy still fails at `ghcr.io/yuan-lab-llm/skill-scanner:latest` because the official image has no `linux/arm64` manifest; local validation currently requires the documented live-only import + `kubectl set image` override path | `/tmp/p1-pkt19-apply-default.txt`; `/tmp/p1-pkt19-overrides.txt` | Gate 1 stays `BLOCKED`; the tutorial path must explicitly mark this as local validation only and must not describe it as repo-default truth |
| Gate 2 boundary | after the live-only ARM overrides, fresh bring-up stops at the documented operator model-bootstrap prerequisite because `/api/v1/admin/models` is still empty until the operator creates one active normal model and one active secure model | `/tmp/p1-pkt19-bringup.txt`; `/tmp/p1-pkt19-verdict.txt` | the from-zero tutorial cannot honestly claim validation-instance or direct-QA readiness until the operator model step is completed |
| Gate 3 blocker | local ARM64 dev candidate live override made `skill-scanner` runnable, and a real agent session successfully posted `POST /api/v1/agent/skills/inventory`, but the runtime inventory was empty so no skill rows or package-collection commands were created | `/tmp/p1-pkt16b-rollout.txt`; `/tmp/p1-pkt17a-verdict.txt`; `/tmp/p1-pkt18b-runtime-or-agent-proof.txt`; `/tmp/p1-pkt18b-after-state.txt` | Commander cannot start full `U3` validation from Source B until the product path surfaces non-empty runtime skills or the Source B expectation is re-scoped |
