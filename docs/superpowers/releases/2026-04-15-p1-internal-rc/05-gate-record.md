# Gate Record

| Gate | Name | Status | Acceptance Rows | Canonical Evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| Gate 1 | Repeatable deploy | BLOCKED | 7.1 | `06-evidence-index.md` | Deploy preflight frozen in `01-deploy-preflight.md`; K3S client dry-run captured in `/tmp/p1-k3s-dryrun.txt`; `P1-PKT-19` then replayed from a fresh cluster and reproduced the repo-default Mac/ARM boundary: `ghcr.io/yuan-lab-llm/skill-scanner:latest` fails with `no match for platform in manifest: not found`. The documented live-only ARM override path can unblock local validation, but it does not promote repo-default or fresh-deploy truth to PASS. |
| Gate 2 | Repeatable startup | BLOCKED | 7.2 | `06-evidence-index.md` | `P1-PKT-19` showed that after the live-only ARM overrides, fresh bring-up reaches `mysql` / `clawmanager-app` / `skill-scanner` `Running`, `https://localhost:30443/healthz` returns `200`, admin login works, and the OpenClaw ARM dev bootstrap image is visible via API. The replay then stops honestly at the documented operator model-bootstrap boundary because no active normal model or active secure model had been provided. |
| Gate 3 | Capability completeness | BLOCKED | 7.3 | `06-evidence-index.md` | Source A is frozen in `03-u3-task-inventory.md` at 0 rows. `P1-PKT-16B` unlocked a live-only local ARM64 dev candidate override for `skill-scanner` and `/api/v1/admin/security/config` became reachable, but `/tmp/p1-task5-admin-skills.json` still returned `0` rows, so no Source B rows could be appended and `L3` does not start. `P1-PKT-17A` verified that `/api/v1/admin/skills` is a DB-backed `skills` list and that the current live P1 path has not executed any legal skill-row creation path. `P1-PKT-18B` then exercised `POST /api/v1/agent/skills/inventory` with a real agent session on instance 6, but the runtime reported an empty inventory and the skill tables remained at `0`, so Source B still does not surface under the currently observed product path. This does not change repo-default or fresh-deploy truth. |
| Gate 4 | Operability | BLOCKED | 7.5 | `06-evidence-index.md` | |
| Gate 5 | Release closure | BLOCKED | 7.4, 7.5 | `06-evidence-index.md` | |
